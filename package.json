{
	"workspaces": [
		"apps/**",
		"packages/**"
	],
	"scripts": {
		"dev": "cd test/app && pnpm dev",
		"modal-manager:release": "cd packages/modal-manager && pnpm version patch && pnpm publish",
		"modal-manager:build": "cd packages/modal-manager && pnpm build",
		"modal-manager:build:watch": "cd packages/modal-manager && pnpm build:watch"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"prettier-plugin-svelte": "^3.1.2"
	},
	"dependencies": {
		"prettier-plugin-tailwindcss": "^0.5.9"
	}
}
